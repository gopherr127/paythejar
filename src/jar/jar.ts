export interface Jar {
    name: string;
    category: string;
    //timeStored: number;
}

export interface JarFilter {
    name: string;
    category: string;
    /* totalAmount: {
        lower: number,
        upper: number
    }; */
}